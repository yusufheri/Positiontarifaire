<form method="post" action="{{ path('dashboard_colonne_delete', {'id': colonne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this column ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colonne.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Supprimer</button>
</form>
