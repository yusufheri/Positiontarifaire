{{ form_start(form, {class: 'form-horizontal'}) }}
    <div class="form-group">
        <label for="selphoto" class="col-sm-12 control-label">Sélectionnez le fichier à importer:</label>
        <div class="col-sm-12">
            {{ form_row(form.excelFile, {'attr' : {'class' : 'inputstl', 'id':'selphoto'}}) }}
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-12">
            <div class="row">
                <div class="col-md">
                    <button type="submit" class="btn btn-block btn-success" id="btnSubmit"><i class="fa fa-check"></i> Enregistrer</button>
                </div>
                <div class="col-md">&nbsp;</div>
                <div class="col-md">
                    <button type="button" id="illustration" class="btn btn-block btn-primary" style="float:rigth;"><i class="fa fa-picture-o" aria-hidden="true"></i> Illustration</button>
                </div>
            </div>
        
        
        </div>
    </div>
{{ form_widget(form) }}
{{ form_end(form) }}