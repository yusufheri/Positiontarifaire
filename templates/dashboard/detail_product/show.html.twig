{% extends 'base.html.twig' %}

{% block title %}DetailProduct{% endblock %}

{% block body %}
    <h1>DetailProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_product.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ detail_product.createdAt ? detail_product.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ detail_product.name }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ detail_product.deletedAt ? detail_product.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dashboard_detail_product_index') }}">back to list</a>

    <a href="{{ path('dashboard_detail_product_edit', {'id': detail_product.id}) }}">edit</a>

    {{ include('dashboard/detail_product/_delete_form.html.twig') }}
{% endblock %}
