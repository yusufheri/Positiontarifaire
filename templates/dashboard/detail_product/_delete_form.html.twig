<form method="post" action="{{ path('dashboard_detail_product_delete', {'id': detail_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_product.id) }}">
    <button class="btn">Delete</button>
</form>
